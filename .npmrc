# Expo / React Native + pnpm: use a hoisted node_modules layout so Metro and
# the Expo config plugins resolve dependencies the way they expect (like npm/yarn).
# This avoids a whole class of "unable to resolve module" errors in RN monorepos.
node-linker=hoisted
